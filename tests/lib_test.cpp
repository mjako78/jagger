#include <stdio.h>

#include "doctest/doctest.h"
#include "jagger/jagger.h"

TEST_SUITE("meaningful suite :)") {
  // TEST_CASE("console" * doctest::description("console logging only")) {
  //   CHECK(jagger_init(LOG_MODE_CONSOLE, LOG_LEVEL_DEBUG, NULL) == 1);
  // }
}
